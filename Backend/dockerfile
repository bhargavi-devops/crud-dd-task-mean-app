FROM node:18-alpine

WORKDIR /usr/src/app

# install dependencies (use package-lock if present)
COPY backend/package*.json ./
RUN npm ci --only=production

COPY backend/ ./

ENV PORT=3000
EXPOSE 3000

CMD ["node", "index.js"]
